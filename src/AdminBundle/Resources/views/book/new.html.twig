{% extends 'adminbase.html.twig' %}

{% block body %}
    <div class="row">
        <div class="col-xs-12">
            <h1>Adicionar de livro</h1>

            {{ form_start(form) }}
                {{ form_widget(form) }}
            {{ form_end(form) }}

            <ul>
                <li>
                    <a href="{{ path('book_index') }}">Voltar para a lista</a>
                </li>
            </ul>
        </div>
    </div>
{% endblock %}
